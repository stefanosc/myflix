- description ||= nil
%article.video_category
  %header
    %h3= link_to "#{category.name}", category_path(category)
  - if description
    %p.well= "#{description}"
  .videos.row  
    = render category.videos